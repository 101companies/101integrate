\begin{table}
    \centering
    \begin{tabular}{lcr}
    \hline
    {% for col in head %}
        \textbf{ {{col}} }
        {% if not forloop.last %}
           &
        {% endif %}
    {% endfor %} 
    \\
    \hline
    {% for row in table %}
        {% for cell in row %}
            {{cell}}
            {% if not forloop.last %} 
               &
            {% endif %}
        {% endfor %}
        \\
    {% endfor %}
    \hline
    \end{tabular}
    \caption{% verbatim %}{{% endverbatim %}
        {{ caption }}
    {% verbatim %}}{% endverbatim %}
    \label{% verbatim %}{{% endverbatim %}
        {{ label }}
    {% verbatim %}}{% endverbatim %}
\end{table}